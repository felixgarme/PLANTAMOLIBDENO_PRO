// suponiendo que “app” es tu instancia de Verge3D
const controls = app.controls;

// Si cámara perspectiva:
controls.minDistance = 100;     // distancia mínima a la que la cámara puede acercarse
controls.maxDistance = 900;    // distancia máxima a la que la cámara puede alejarse
controls.enableZoom = true;
